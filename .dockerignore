Dockerfile
Dockerfile.dev
.dockerignore

# Node dependencies — the deps stage installs these fresh with yarn; a local
# node_modules (e.g. from testing with npm on a different OS) must never be
# copied in, or it silently overwrites the correct platform-native binaries.
node_modules
package-lock.json

# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Git metadata
.git
.gitignore

# Logs
logs
*.log

# Local env files
.env
.env.*
!.env.example

.dev.vars
.wrangler

.auth
public/replay-assets/

.vs/
