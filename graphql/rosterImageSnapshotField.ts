// generated/zeus predates the nullable historical roster snapshot columns.
// Keep the compatibility escape hatch narrow instead of regenerating the
// complete schema output for two read-only fields.
export const rosterImageSnapshotField: any = {
  roster_image_url_snapshot: true,
};
